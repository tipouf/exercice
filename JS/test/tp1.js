var saisie = parsint('010', prompt("Veuillez rentrer un nombre entre 0 et 999 : "));

function convert(saisie){
 if (isNaN(saisie) || saisie > 999 || saisie < 0) {
     alert("Veuillez rentrer un nombre entre 0 et 999");
 } else if (saisie > 99) {
        
 }
 }

